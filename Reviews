import { motion } from "framer-motion";
import { Quote, Star } from "lucide-react";

const reviews = [
  {
    name: "Lenny Rothrock",
    text: "Professionalism from the start — this is truly what a roofing company should be. The crew worked all day until dark to make sure we were dried in. Fair, honest prices and they did everything they said they would. AMAZING.",
    role: "Roof installation",
  },
  {
    name: "Allison DelPino",
    text: "There are not enough words to describe how great this company is. They honored the quote regardless of material costs, were on time with the insurance adjuster, and walked me through the entire process.",
    role: "Realtor & Homeowner",
  },
  {
    name: "Laura Lopp",
    text: "Mr. Garrett came and checked my roof, scheduled it for the next day, and was so kind. Home repair was new to me after my husband passed — he made the whole process easy.",
    role: "Roof repair",
  },
];

export function Reviews() {
  return (
    <section id="reviews" className="relative py-32 px-6 overflow-hidden">
      <div className="absolute inset-0 bg-gradient-radial opacity-50" />
      <div className="relative max-w-7xl mx-auto">
        <motion.div
          initial={{ opacity: 0, y: 24 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.8 }}
          className="text-center mb-20"
        >
          <span className="text-xs uppercase tracking-[0.3em] text-copper">Word of mouth</span>
          <h2 className="mt-4 font-display text-5xl sm:text-6xl text-cream text-balance max-w-3xl mx-auto">
            "Outperforms every other company we've worked with."
          </h2>
        </motion.div>

        <div className="grid md:grid-cols-3 gap-6">
          {reviews.map((r, i) => (
            <motion.figure
              key={r.name}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true, margin: "-50px" }}
              transition={{ duration: 0.7, delay: i * 0.12 }}
              className="bg-card border border-border rounded-2xl p-8 flex flex-col hover:border-copper/50 transition-colors"
            >
              <Quote className="h-8 w-8 text-copper/60" />
              <blockquote className="mt-4 text-cream/90 leading-relaxed text-[15px] flex-1">
                {r.text}
              </blockquote>
              <figcaption className="mt-6 pt-6 border-t border-border flex items-center justify-between">
                <div>
                  <div className="font-display text-cream">{r.name}</div>
                  <div className="text-xs text-muted-foreground">{r.role}</div>
                </div>
                <div className="flex">
                  {[...Array(5)].map((_, j) => (
                    <Star key={j} className="h-3.5 w-3.5 fill-copper text-copper" />
                  ))}
                </div>
              </figcaption>
            </motion.figure>
          ))}
        </div>
      </div>
    </section>
  );
}
